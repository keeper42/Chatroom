<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:id="@+id/nav_group_main" android:checkableBehavior="single">
        <item
                android:id="@+id/nav_home"
                android:title="@string/nav_menu_home"
                android:icon="@drawable/ic_menu_home"/>

    </group>

    <group android:id="@+id/nav_group_setting" android:checkableBehavior="single">
        <item
                android:id="@+id/nav_settings"
                android:title="@string/nav_menu_setting"
                android:icon="@drawable/ic_menu_setting"/>
    </group>

</menu>
